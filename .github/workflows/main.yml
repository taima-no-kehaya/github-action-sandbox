name: file check
on: push

jobs:
  flie-check:
    runs-on: ubuntu-latest
    steps:
      - name: test1
        id: test1
        run: exit 1
      - name: test2
        run: echo test2
      - name: test3
        if: failure()
        run: echo test3
